{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "aws s3 sync out/ s3://edulog-web --delete  --exact-timestamps",
    "export": "next export"
  },
  "dependencies": {
    "@aws-amplify/ui-react": "^6.13.0",
    "aws-amplify": "^6.15.7",
    "next": "15.5.4",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "recharts": "^3.2.1"
  }
}
